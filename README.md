# Candidate True Companion

Resume in, one realistic interview, useful feedback out.

Start with `CLAUDE.md` for project context and conventions, and `docs/Architecture-Decisions.md` for the technical design (living ADR). Full documentation is deferred until later in the build — the ADR is the source of truth until then.
